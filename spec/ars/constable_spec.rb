require "spec_helper"

RSpec.describe Ars::Constable do
  it "has a version number" do
    expect(Ars::Constable::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
